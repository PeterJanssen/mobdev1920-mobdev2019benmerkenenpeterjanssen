<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="be.pxl.mobdev2019.cityWatch">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".account_activities.DisplayNameActivity"></activity>
        <activity android:name=".account_activities.AccountActivity" />
        <activity android:name=".report_activities.ListOfReportsListActivity" />
        <activity android:name=".account_activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".account_activities.LoginActivity" />
        <activity android:name=".account_activities.CreateAccountActivity" />
        <activity
            android:name=".PersonalReportsListActivity"
            android:label="@string/title_activity_personal_reports_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
    </application>

</manifest>